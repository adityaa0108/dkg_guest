/**
 * Header Component
 * Main navigation header
 */

'use client'

export default function Header() {
  return (
    <header>
      <div className="container">
        <div className="logo">Event Booking Platform</div>
        <nav>
          {/* Navigation links */}
        </nav>
        <div className="actions">
          {/* Search, Login, etc. */}
        </div>
      </div>
    </header>
  )
}
